#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 on success
 */
int main(void)
{
	int i;

	printf("Hi, Checker!\n");

	for (i > 1; i < 10; i++)
	{
		printf("Single digit Integer  %d\n", i);
	}

	return (0);
}
