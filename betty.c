#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 on success
 */
int main(void)
{
	int i;

	printf("Hi, Checker!\n");

	for (i > 0; i < 10; i++)
	{
		printf("Single digit Integers  %d\n", i);
	}

	return (0);
}
